<% layout("layouts/Boilerplate.ejs")%>
<body>

    


    <div class="offset-3">
        
    <div class="card " style="width: 18rem; ">
        <h3>Listing Details:</h3>
        
 <img src="<%= listing.image %>"
     class="card-img-top mx-auto d-block"
     alt="listing image"
     style="width: 40vw; object-fit: cover;">

  <div class="card-body">
    <h5 class="card-title"></strong> <%= listing.title %></h5>
    <p class="card-text"></strong> <%= listing.description %></p>
    <p class="card-text"></strong> ₹ <%= listing.price.toLocaleString("en-IN") %></p>
    <p class="card-text"></strong> <%= listing.location %></p>
    <p class="card-text"></strong> <%= listing.country %></p>
    
   <div class="but">
     <form method="get" action="/listings/<%= listing._id %>/edit">
    <button type="submit" class="btn btn-dark">Update Listing</button>
</form>

<form method="post"
      action="/listings/<%= listing._id %>/delete"
      onsubmit="return confirm('Are you sure you want to delete this listing?');">
    <button type="submit" class="btn btn-danger">Delete Listing</button>
</form>
</div>
  </div>
  </div>
</div>

  
</body>

